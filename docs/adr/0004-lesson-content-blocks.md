# ADR 0004：课时采用有序内容块

- 状态：已接受
- 日期：2026-08-03

## 决策

课时正文拆分为带类型和顺序的 `LessonContentBlock`，不使用单一大文本字段。Markdown 和公式采用安全渲染链路。

## 原因

块模型支持课程内公式、提示、例题、案例、对比表和课中练习的差异化展示，也便于重排、复用、增量加载和版本审计。

## 后果

发布前需要执行块类型校验、序号校验和引用完整性校验。前端需要内容块注册表，但无需为每节课定制页面。

