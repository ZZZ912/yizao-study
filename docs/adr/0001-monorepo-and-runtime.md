# ADR 0001：单仓库与生产运行时

- 状态：已接受
- 日期：2026-08-03

## 决策

前端、后端、部署脚本与文档存放在同一仓库。生产环境由 Caddy 提供 React 构建产物并反向代理 Gunicorn；PostgreSQL 只存在于内部容器网络。

## 原因

第一阶段团队规模小，单仓库能让 API、部署和文档变更保持同步。静态前端不需要长期 Node 服务，可降低 4 GB 服务器的内存消耗和运维面。

## 后果

CI 需要同时验证 Python 与 Node 工具链。生产镜像需要多阶段构建，但运行容器更少、网络边界更清晰。

